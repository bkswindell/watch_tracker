# Phase 1 builder status

**Checkpoint:** `c0fab3442155f57f933ae0161508cc1fa6caac7f` — `fix(security): align login throttle baseline`

- **Delivered auth/session gate:** exact scheme/host/port Origin validation in addition to token CSRF; bounded ten-attempt/15-minute per-client login throttling; authenticated CSRF-protected logout with server-side session invalidation and cookie expiry; 30-day sliding idle and 90-day absolute session lifetimes; Argon2id fresh credentials at `m=65536`, `t=3`, `p=1` with the accepted password policy. Login throttle state is capped at 10,000 active entries.
- **This checkpoint:** corrected the production default from five to the reviewed ten failed logins per remote address in 15 minutes, followed by a 15-minute cooldown. The focused regression exercises ten `401` failures then requires `429 auth.throttled`; tests now omit their throttle override when verifying the actual application default.
- **Validation:** focused auth/SQL suite passed 12/12; `npm run check` passed all 108 portable tests; deterministic build passed with 25 artifacts; `npm audit --omit=dev --audit-level=high` reported 0 vulnerabilities; Dockerfile check and Compose configuration validation passed. Host PostgreSQL integration was attempted but failed closed because `TEST_DATABASE_URL` is not available; CI will run the isolated PostgreSQL gate.
- **Deployment/API proof:** rebuilt image `watch-tracker@sha256:411ecda5d3da584047404c9425518018f47d2154066993460e65e67357be8d81`; recreated only `watch-tracker-lan-foundation-app-1` with `--no-deps`. The existing database container remained healthy and untouched. The deployed app is healthy; `/ready` returned ready and credential-free `/api/bootstrap` returned `Cache-Control: no-store`.
- **Remote/CI state:** local HEAD and `origin/automation/phase1` were verified at the implementation/progress parent. Validate Core run [`32792198422`](https://github.com/bkswindell/watch_tracker/actions/runs/32792198422) passed its locked dependency checks, production/full audits, formatting/type/lint, 108 portable tests, isolated PostgreSQL integration and migration identity, deterministic build, Docker/Compose validation, image scans, CycloneDX SBOM, and disposable Compose smoke.
- **Browser proof:** no deployment credential was accessed. Authenticated browser proof remains unavailable in this cron environment and was not fabricated; focused API proof covers this backend-only correction.
- **Remaining accepted auth gate:** host-controlled recovery. The decision needed is an approved host-managed delivery channel (for example SMTP relay or help-desk/admin-mediated reset) and the user-facing recovery UX. No host credentials, PostgreSQL container, or volume were changed to infer it.
- **Historical audit:** remains **FAIL** until host-controlled recovery and every accepted auth/session gate passes.
