# Phase 1 builder status

**Checkpoint:** `31522a6fd7e1681ae4a3edc46709647c98326173` — `fix(auth): require safe recovery delivery URL`

- **Delivered recovery gate:** the repository-conventional host-admin CLI creates a 256-bit random, 15-minute, fragment-only reset link and writes it exactly once to stdout. PostgreSQL stores only SHA-256 digests bound to the singleton Tracker instance; issuing a link atomically invalidates prior outstanding links. Completion atomically claims a valid unexpired token, applies the existing Argon2id/password policy, updates the credential, and deletes all sessions. Invalid, expired, superseded, reused, cross-owner, malformed, and policy-invalid submissions use the same generic API failure.
- **Final audit corrections:** independent review found that the CLI allowed a non-loopback plaintext HTTP base URL and validated the delivery URL after issuance. The CLI now requires HTTPS except for `localhost`, IPv4 `127/8`, and `[::1]`, and validates the complete delivery URL before opening the database pool or invalidating/issuing a token. Regression coverage includes loopback IPv4/IPv6 and remote-HTTP rejection plus validation-before-mutation ordering.
- **UI/security behavior:** `/reset-password` is a production-served SPA entry with `Cache-Control: no-store` and `Referrer-Policy: no-referrer`. The React UI reads the URL fragment once, immediately replaces the URL, keeps the token only in memory, sends it only in the same-origin POST body, clears reset/password state after an attempt, uses new-password autocomplete, and does not use browser storage, logs, analytics, or query strings. Login authentication/session creation remains serialized against recovery.
- **Validation:** `npm run check` passed 115 portable tests plus formatting, typecheck, lint, and production builds. Deterministic build passed with 27 artifacts. Production and full `npm audit` returned zero vulnerabilities. Dockerfile check and Compose config passed; image `watch-tracker:verify` is `sha256:bd1da6b60f64f09cbd2051bc7546281b9211f74e01da3e0b73b256f17dfa35b9`.
- **CI/remote:** Core PR #4 exact implementation head is `31522a6fd7e1681ae4a3edc46709647c98326173`; Validate Core run [`32797219701`](https://github.com/bkswindell/watch_tracker/actions/runs/32797219701) passed all required gates, including isolated PostgreSQL recovery/integrity tests, migration identity, image scans, SBOM, and disposable Compose smoke. Template PR #10 remains green at `ec5bcc16517809acce74a1b876dc105c50f3f431`.
- **Deployment:** only `watch-tracker-lan-foundation-app-1` was recreated with `--no-deps`. It is healthy on the current image and `/ready` returned ready. Database container `9167a92718aa…`, its creation time, health, and volume `watch-tracker-lan-foundation_postgres_data` were preserved; no migration, durable data, volume, or credential changed.
- **Authenticated browser proof:** Microsoft Edge `151.0.4129.86` authenticated against the deployed latest implementation with the existing private host credential, which was neither printed nor changed. The live workspace showed Administrator, Lantern Vale, and Sign out; bootstrap, login, workspace, catalog-additions, and feedback requests returned successful responses with `Cache-Control: no-store`; the session cookie was HttpOnly and SameSite=Strict; browser local/session storage remained empty. The secret-free screenshot is retained outside the repository at `/tmp/watch-tracker-browser-proof/latest-head-authenticated.png` with SHA-256 `f0d71c1ca5e1c56cd409fc3ac9aec4a89345d7082b88b2bf5b5e29494d545bc5`.
- **Independent recovery audit:** final read-only review passed the explicit approved recovery/auth design with no blocking or material finding after both URL corrections. Operator-side stdout handling and the requirement for HTTPS off loopback remain documented operational controls.
- **Release state:** the historical overall Phase 1 release audit remains **FAIL** pending broader Phase 1 closeout outside this recovery/auth gate. PRs remain open; no release, tag, default-branch merge, credential rotation, or destructive operation occurred.
