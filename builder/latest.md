# Phase 1 builder status

**Checkpoint:** `ec00a1afd3ec682a322d96cdc7a00784d45bdd1a` — `fix(auth): require browser origin for reset completion`

- **Delivered recovery/auth gate:** a repository-conventional host-admin CLI emits one 256-bit random, 15-minute, fragment-only reset link. PostgreSQL stores only a SHA-256 digest bound to the Tracker instance; issuance atomically supersedes earlier outstanding links. Completion atomically consumes a valid unexpired token, applies the established Argon2id/password policy, and revokes every existing session. Invalid, expired, superseded, reused, malformed, cross-owner, and policy-invalid submissions return the same generic failure.
- **Delivery protections:** the CLI validates its entire base URL before opening the database pool or mutating reset state, requires HTTPS except for explicit loopback hosts, emits the link only once to invoking-admin stdout, and does not persist or log it. `/reset-password` is served with `Cache-Control: no-store` and `Referrer-Policy: no-referrer`; the UI immediately removes the fragment and keeps the token only in memory.
- **Current validation:** focused recovery tests (5/5) and `npm run check` passed all 115 portable tests plus formatting, typecheck, lint, and production builds. `npm audit --audit-level=high` reported zero vulnerabilities; deterministic production build passed with 27 artifacts. The new head's Validate Core run [`32798734680`](https://github.com/bkswindell/watch_tracker/actions/runs/32798734680) is in progress.
- **CI/remote:** Core PR #4 is open and points exactly to `ec00a1afd3ec682a322d96cdc7a00784d45bdd1a`; its Validate Core run [`32798734680`](https://github.com/bkswindell/watch_tracker/actions/runs/32798734680) is in progress. The preceding recovery head's run [`32798000470`](https://github.com/bkswindell/watch_tracker/actions/runs/32798000470) passed, including isolated PostgreSQL recovery/integrity and migration-identity tests, production/full audits, image scans, SBOM, and disposable Compose smoke. Template PR #10 remains green at `ec5bcc16517809acce74a1b876dc105c50f3f431`.
- **Live verification:** only `watch-tracker-lan-foundation-app-1` is healthy after the prior app-only `--no-deps` deployment; its paired database remains healthy and untouched. `/health` is healthy; `/reset-password` returns no-store/no-referrer/restrictive CSP; a credential-free malformed completion request returned only generic `400 password-reset.invalid`.
- **Browser certification:** prior latest-implementation authenticated Edge proof remains recorded with secret-free screenshot hash `f0d71c1ca5e1c56cd409fc3ac9aec4a89345d7082b88b2bf5b5e29494d545bc5`; a new cron-harness invocation cannot launch Chrome, so no new browser assertion was fabricated and no credential was accessed.
- **Release state:** the historical overall Phase 1 audit remains **FAIL** pending broader Phase 1 closeout. PRs remain open; no release, tag, default-branch merge, credential rotation, force-push, migration, or destructive operation occurred.
