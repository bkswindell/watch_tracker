# Phase 1 builder status

**Checkpoint:** `b8cb598071fb4806351b2e43587af79527cfa8ac` — `fix(auth): reject decorated Origin headers`

- **Delivered recovery/auth gate:** the repository-conventional host-admin CLI emits one 256-bit random, 15-minute, fragment-only reset link. PostgreSQL stores only a SHA-256 digest bound to the Tracker instance; issuance atomically supersedes earlier outstanding links. Completion atomically consumes a valid unexpired token, applies the established Argon2id/password policy, revokes every existing session, and now consumes a valid token after five policy-invalid submissions as required by the non-negotiable Phase 1 security baseline.
- **Latest hardening:** exact Origin validation now rejects decorated URL values—including userinfo, a path, query, or fragment—even if their parsed host otherwise matches. Recovery completion accepts only the exact two-field JSON contract and a 43-character 256-bit base64url token. Extra fields, arrays, prototype-bearing input, malformed tokens, overlong passwords, malformed JSON, and oversized request bodies all fail through the same generic, no-store/no-referrer response. The PostgreSQL gate also proves a successful reset revokes only the bound owner's sessions while preserving another owner's session.
- **Delivery protections:** the CLI validates its entire base URL before opening the database pool or mutating reset state, requires HTTPS except for explicit loopback hosts, emits the link only once to invoking-admin stdout, and does not persist or log it. `/reset-password` is served with `Cache-Control: no-store` and `Referrer-Policy: no-referrer`; the UI immediately removes the fragment and keeps the token only in memory. Invalid, expired, superseded, exhausted, reused, malformed, cross-owner, and policy-invalid submissions remain generic.
- **Validation:** focused recovery tests passed 5/5; `npm run check` passed all 116 portable tests plus formatting, typecheck, lint, and production builds. Production/full high-severity audit reported zero vulnerabilities; deterministic production build passed with 27 artifacts; Dockerfile and Compose checks passed. CI is running at exact delivery head [`b8cb598`](https://github.com/bkswindell/watch_tracker/commit/b8cb598071fb4806351b2e43587af79527cfa8ac); the prior Core Validate run [`32801356501`](https://github.com/bkswindell/watch_tracker/actions/runs/32801356501) passed.
- **CI/remote:** Core PR #4 remains open at exact `b8cb598071fb4806351b2e43587af79527cfa8ac`, matching `origin/automation/phase1`; its Validate Core run [`32801791828`](https://github.com/bkswindell/watch_tracker/actions/runs/32801791828) is in progress. Template PR #10 remains open and green at `ec5bcc16517809acce74a1b876dc105c50f3f431`.
- **Live deployment:** additive migration `0.10` remains current; no credential changed. Only the app was rebuilt and recreated with `--no-deps` on image `sha256:7826d850bff52db06faac1a03463c7f1429c8daf9fcf558377019bf2ad804ce6`. Database container `9167a92718aa2107daa710f998156cc6b4ed47d9e513828bbc70a13d172955d5`, its creation time, volume, and health were preserved. `/ready` is ready; a live decorated-Origin completion returns generic `400 password-reset.invalid` with no-store/no-referrer.
- **Browser evidence:** the prior Edge certification remains secret-free evidence for the preceding deployed checkpoint; no new authenticated browser claim is made for this API-only hardening.
- **Release state:** the recovery gate now passes, but the historical overall Phase 1 audit remains **FAIL** pending broader Phase 1 closeout and exact-final-diff governance. PRs remain open; no release, tag, default-branch merge, credential rotation, force-push, destructive durable-data operation, or OxyGen/port 3000 change occurred.
