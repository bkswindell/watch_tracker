# Phase 1 builder status

**Checkpoint:** `ddec0a87d8b6c260bd74f38275ca3ee22cda79b4` — `fix(auth): require exact reset fragment token`

- **Delivered recovery/auth gate:** the repository-conventional host-admin CLI emits one 256-bit random, 15-minute, fragment-only reset link. PostgreSQL stores only a SHA-256 digest bound to the Tracker instance; issuance atomically supersedes earlier outstanding links. Completion atomically consumes a valid unexpired token, applies the established Argon2id/password policy, revokes every existing session, and consumes a valid token after five policy-invalid submissions.
- **Latest correction:** the reset UI accepts only the exact host-admin fragment contract: one `token` key carrying one 43-character base64url value. Duplicate token keys, companion fragment parameters, malformed values, and unrelated fragments become an empty in-memory value and follow the existing generic failure path. The fragment is still removed immediately and a valid token remains only in component memory for a retry after a generic API failure.
- **Delivery protections:** exact Origin validation rejects userinfo, paths, queries, fragments, and scheme/host/port mismatches. Recovery accepts only the exact two-field JSON contract and a 43-character base64url token. Parser, invalid, expired, superseded, exhausted, reused, malformed, cross-owner, and policy failures remain generic and no-store/no-referrer. The UI removes the URL fragment immediately and never uses browser storage, query strings, analytics, or logs for the token.
- **Validation:** focused recovery tests passed 6/6; `npm run check` passed all 117 portable tests plus formatting, typecheck, lint, and production builds. Production/full high-severity audits reported zero vulnerabilities; deterministic production build passed with 27 artifacts. Validate Core run [`32803174337`](https://github.com/bkswindell/watch_tracker/actions/runs/32803174337) passed the isolated PostgreSQL/migration, image-scan, SBOM, and disposable Compose smoke gates at the exact PR head.
- **CI/remote:** Core PR #4 is open and clean at `91c2d17977910ce6cd6e75dbca0acdd6571a54a9`, matching `origin/automation/phase1`; its implementation parent is `ddec0a87d8b6c260bd74f38275ca3ee22cda79b4`. Template PR #10 remains open and green at `ec5bcc16517809acce74a1b876dc105c50f3f431`.
- **Live deployment:** only the app was rebuilt and recreated with `--no-deps` on image `sha256:e44f85f3f96db7cb947b1558b6499e96e618df9f5cb1825705331aa8b0ae2bfc`. It is healthy and `/ready` is ready. The live invalid synthetic token returned generic `400 password-reset.invalid` with no-store/no-referrer. Database container `9167a92718aa2107daa710f998156cc6b4ed47d9e513828bbc70a13d172955d5`, creation time `2026-08-24T16:08:34.899650189Z`, volume `watch-tracker-lan-foundation_postgres_data`, migration state, durable data, and credentials were preserved.
- **Browser evidence:** the preceding deployed checkpoint retains secret-free authenticated and retry evidence. A fresh browser-harness attempt could not start Chrome, so no latest-head browser claim is made for this hardening.
- **Release state:** the recovery gate passes, but the historical overall Phase 1 audit remains **FAIL** pending broader Phase 1 closeout and exact-final-diff governance. PRs remain open; no release, tag, default-branch merge, credential change, force-push, destructive durable-data operation, or OxyGen/port 3000 change occurred.
