# Phase 1 builder status

**Checkpoint:** `a8974d8bfd67bec69d62ad4f21e9effcde267bb9` — `fix(auth): genericize reset parser failures`

- **Delivered recovery/auth gate:** the repository-conventional host-admin CLI emits one 256-bit random, 15-minute, fragment-only reset link. PostgreSQL stores only a SHA-256 digest bound to the Tracker instance; issuance atomically supersedes earlier outstanding links. Completion atomically consumes a valid unexpired token, applies the established Argon2id/password policy, revokes every existing session, and now consumes a valid token after five policy-invalid submissions as required by the non-negotiable Phase 1 security baseline.
- **Latest hardening:** recovery completion accepts only the exact two-field JSON contract and a 43-character 256-bit base64url token. Extra fields, arrays, prototype-bearing input, malformed tokens, overlong passwords, malformed JSON, and oversized request bodies now all fail through the same generic, no-store/no-referrer response. The PostgreSQL gate also proves a successful reset revokes only the bound owner's sessions while preserving another owner's session.
- **Delivery protections:** the CLI validates its entire base URL before opening the database pool or mutating reset state, requires HTTPS except for explicit loopback hosts, emits the link only once to invoking-admin stdout, and does not persist or log it. `/reset-password` is served with `Cache-Control: no-store` and `Referrer-Policy: no-referrer`; the UI immediately removes the fragment and keeps the token only in memory. Invalid, expired, superseded, exhausted, reused, malformed, cross-owner, and policy-invalid submissions remain generic.
- **Validation:** `npm run check` passed all 116 portable tests plus formatting, typecheck, lint, and production builds. Isolated PostgreSQL integration passed 2/2, including recovery concurrency, owner isolation, and session-revocation isolation. Production/full high-severity audits reported zero vulnerabilities; deterministic production build passed with 27 artifacts; Dockerfile and Compose checks passed. Validate Core run [`32801191779`](https://github.com/bkswindell/watch_tracker/actions/runs/32801191779) passed at exact delivery head `c765cdd5d402f95902edfe245dded7c2c9dbe604`, including PostgreSQL, migration identity, audits, image scans, SBOM, and Compose smoke.
- **CI/remote:** Core PR #4 remains open and green at delivery head `c765cdd5d402f95902edfe245dded7c2c9dbe604`; implementation is `a8974d8bfd67bec69d62ad4f21e9effcde267bb9`. Template PR #10 remains open and green at `ec5bcc16517809acce74a1b876dc105c50f3f431`.
- **Live deployment:** additive migration `0.10` remains current; no credential changed. Only the app was rebuilt and recreated with `--no-deps` on image `sha256:56c641e18c15f6222814bfa76cb1cdd2c4930eeda323054b07ddbf74fd5fa358`. Database container `9167a92718aa2107daa710f998156cc6b4ed47d9e513828bbc70a13d172955d5`, its creation time, volume, and health were preserved. `/ready` is ready; live malformed JSON completion returns generic `400 password-reset.invalid` with no-store/no-referrer.
- **Latest-implementation browser proof:** Microsoft Edge `151.0.4129.86` displayed the authenticated Administrator workspace and Sign out against the deployed implementation. `/api/workspace` returned `200` with `Cache-Control: no-store`; the session cookie was HttpOnly and SameSite=Strict; local and session storage were empty. Secret-free screenshot SHA-256: `d0c1602bde010957592ba237f57bb31ca08d235c27cf9dd7c9a794aa50e7e709`.
- **Release state:** the recovery gate now passes, but the historical overall Phase 1 audit remains **FAIL** pending broader Phase 1 closeout and exact-final-diff governance. PRs remain open; no release, tag, default-branch merge, credential rotation, force-push, destructive durable-data operation, or OxyGen/port 3000 change occurred.
