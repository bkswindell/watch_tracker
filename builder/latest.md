# Phase 1 builder status

**Checkpoint:** `bf7bf1b584d9a036dab1658fb17b8aca4366b8f4` — `docs(recovery): align reset token data model`

- **Recovery completion hardening:** successful reset now also expires `watch_tracker_session` in the responding browser (`HttpOnly; SameSite=Strict; Path=/; Max-Age=0`). Server-side session revocation remains atomic and authoritative; this removes a stale client cookie when recovery is initiated from an already signed-in browser. The API regression test asserts the precise deletion cookie alongside no-store/no-referrer and three-session revocation.
- **Delivered recovery/auth gate:** the repository-conventional host-admin CLI emits one 256-bit random, 15-minute, fragment-only reset link. PostgreSQL stores only a SHA-256 digest bound to the Tracker instance; issuance atomically supersedes earlier outstanding links. Completion atomically consumes a valid unexpired token, applies the established Argon2id/password policy, revokes every existing session, and consumes a valid token after five policy-invalid submissions.
- **Documentation reconciliation:** the Phase 1 data dictionary and table budget now name the implemented `password_reset_token` table and its digest-only schema, replacing the obsolete planned `password_recovery_credential` model. This preserves an accurate host-admin recovery handoff without recording a reset secret.
- **Latest verification correction:** recovery tests now execute both CLI outcomes: successful PostgreSQL-backed issuance emits exactly one fragment-only link and persists no plaintext token, while pre-issuance failure emits no stdout and only the generic host-admin error. API coverage proves expired and invalid links have the same public response and headers. Memory and PostgreSQL tests now revoke three concurrent sessions, and PostgreSQL verifies the resulting credential is Argon2id with `m=65536`, `t=3`, `p=1`.
- **Delivery protections:** exact Origin validation rejects userinfo, paths, queries, fragments, and scheme/host/port mismatches. Recovery accepts only the exact two-field JSON contract and a 43-character base64url token. Parser, invalid, expired, superseded, exhausted, reused, malformed, cross-owner, and policy failures remain generic and no-store/no-referrer. The UI removes the URL fragment immediately and never uses browser storage, query strings, analytics, or logs for the token.
- **Latest independent validation:** on the final exact head, `npm run check` passed formatting, both TypeScript checks, lint, all 119 portable tests, and production builds. A fresh disposable PostgreSQL 17 gate passed both integration tests, including recovery's digest-only/atomic session-revocation scenario. `npm run build:deterministic` passed with 27 artifacts; both complete and production-only `npm audit --audit-level=high` scans found zero vulnerabilities; Dockerfile static validation and `docker compose config -q` passed. The Vite build retains its non-failing advisory about a minified client chunk over 500 kB; it is tracked as a performance follow-up, not a security or correctness failure.
- **CI/remote:** GitHub API confirms Core PR #4 is open, clean, and green at exactly `c8b244fb11927969c9bb84203dd1b6c6d933b2c2`, matching `origin/automation/phase1`; Validate Core run [`32805785621`](https://github.com/bkswindell/watch_tracker/actions/runs/32805785621) passed. Template PR #10 remains open, clean, and green at `ec5bcc16517809acce74a1b876dc105c50f3f431`.
- **Live deployment:** rebuilt `watch-tracker:verify` and recreated only `watch-tracker-lan-foundation-app-1` with `--no-deps` on image `sha256:2557bcb5c70607bfd0feaaa6e6e8cf39f7b45a703e4215c2124c26a28f7bc9f2`. It is healthy and `/ready` is ready. Live invalid synthetic-token completion returned generic `400 password-reset.invalid` with no-store/no-referrer. Database container `9167a92718aa2107daa710f998156cc6b4ed47d9e513828bbc70a13d172955d5`, creation time `2026-08-24T16:08:34.899650189Z`, and volume `watch-tracker-lan-foundation_postgres_data` were exactly preserved; no migration, durable-data, or credential operation occurred.
- **Browser evidence:** latest-deployment Microsoft Edge proof used the preserved authenticated session and returned `200`, `authenticated=true`, `setupRequired=false`, `Cache-Control: no-store`, and the rendered workspace. No credential was read or changed. Secret-free screenshot `/tmp/watch-tracker-6e07f50-authenticated.png` has SHA-256 `d0c1602bde010957592ba237f57bb31ca08d235c27cf9dd7c9a794aa50e7e709`.
- **Latest read-only live review:** the deployed application is healthy and `/ready` returned ready. `GET /reset-password` returned `200` with `Cache-Control: no-store`, `Referrer-Policy: no-referrer`, and the restrictive self-only CSP. A synthetic 43-character invalid token submitted with the exact live Origin returned only generic `400 password-reset.invalid` with the same no-store/no-referrer protections. No token issuance, migration, credential, or database operation occurred. The database container remained healthy and was not recreated; only the app container is currently published at `10.18.0.201:3100`. A fresh cron browser-harness attempt is unavailable because no supported Chromium-family browser is running; no newer browser claim is made.
- **Release state:** the recovery gate passes, but the historical overall Phase 1 audit remains **FAIL** pending broader Phase 1 closeout and exact-final-diff governance. PRs remain open; no release, tag, default-branch merge, credential change, force-push, destructive durable-data operation, or OxyGen/port 3000 change occurred.
