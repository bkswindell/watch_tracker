# Phase 1 builder status

**Checkpoint:** `5f446a7216dd0b379080f14ac335d37387108725` — `fix(security): prevent caching owner API state`

- **Delivered auth/session gate:** exact scheme/host/port Origin validation in addition to token CSRF; bounded five-attempt/15-minute per-client login throttling; authenticated CSRF-protected logout with server-side session invalidation and cookie expiry; 30-day sliding idle and 90-day absolute session lifetimes; Argon2id fresh credentials at `m=65536`, `t=3`, `p=1` with the accepted password policy. Login throttle state is capped at 10,000 active entries.
- **Independent review correction:** every `/api` response now returns `Cache-Control: no-store`, including authenticated owner-scoped data and authentication errors. This extends the earlier bootstrap/login/logout protection so personal Watch Tracker state cannot remain in browser or intermediary caches after logout.
- **Validation:** `npm run check` passed all 107 portable tests; deterministic build passed; `npm audit --omit=dev --audit-level=high` reported 0 vulnerabilities; production image build passed. Validate Core run `32791278109` passed the portable and isolated PostgreSQL gates, migration identity, image scans, CycloneDX SBOM, and disposable Compose smoke.
- **Deployment/API proof:** rebuilt image `watch-tracker@sha256:1744e3733f0f0e3f5bbfe6326aa268757347d831adf7ad89464c7467c902db86`; recreated only `watch-tracker-lan-foundation-app-1` with `--no-deps`. The existing database container remained healthy and untouched. The deployed app is healthy; `/ready` returned ready, credential-free `/api/bootstrap` returned `Cache-Control: no-store`, and unauthenticated `/api/workspace` returned `401 auth.required` with `Cache-Control: no-store`.
- **Remote state:** local HEAD, `origin/automation/phase1`, and Core PR #4 were verified at the implementation SHA before the progress update. Template PR #10 remains green at `ec5bcc16517809acce74a1b876dc105c50f3f431`.
- **Browser proof:** no deployment credential was accessed. Authenticated browser proof remains unavailable in this cron environment and was not fabricated; focused API proof covers this API-header correction.
- **Remaining accepted auth gate:** host-controlled recovery. The decision needed is an approved host-managed delivery channel (for example SMTP relay or help-desk/admin-mediated reset) and the user-facing recovery UX. No host credentials, PostgreSQL container, or volume were changed to infer it.
- **Historical audit:** remains **FAIL** until host-controlled recovery and every accepted gate passes.
