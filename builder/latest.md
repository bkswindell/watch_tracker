# Phase 1 builder status

**Checkpoint:** `00cc270222b79f86a7553c7d4b0dedcdf0fdc8a1` — `test(auth): model expired reset credential`

- **Delivered recovery gate:** a repository-conventional host-admin CLI now creates a 256-bit random, 15-minute, fragment-only reset link and writes it exactly once to stdout. PostgreSQL stores only SHA-256 digests bound to the singleton Tracker instance; issuing a link atomically invalidates prior outstanding links. Completion atomically claims a valid unexpired token, applies the existing Argon2id/password policy, updates the credential, and deletes all sessions. Invalid, expired, superseded, reused, cross-owner, malformed, and policy-invalid submissions use the same generic API failure.
- **UI/security behavior:** `/reset-password` is a production-served SPA entry with `Cache-Control: no-store` and `Referrer-Policy: no-referrer`. The React UI reads the URL fragment once, immediately replaces the URL, keeps the token only in memory, sends it only in the same-origin POST body, clears reset/password state after an attempt, uses new-password autocomplete, and does not use browser storage, logs, analytics, or query strings. Login authentication/session creation is serialized against recovery so an in-flight old-password login cannot survive reset revocation.
- **Schema/API/CLI/UI tests:** migration `0.09` and schema integrity checks cover digest format, owner FK, expiry, and one-use state. Portable tests cover token entropy, 15-minute expiry, supersession, concurrent single use, session revocation, old/new password behavior, generic failures, exact Origin, no-store/no-referrer, fragment-only CLI URLs, once-only stdout, no browser storage/logging, and the production reset-page fallback. PostgreSQL integration covers digest-only persistence, supersession, concurrent atomic use, owner isolation, expiry, credential replacement, and session deletion.
- **Validation:** `npm run check` passed 114 portable tests plus formatting, typecheck, lint, and production builds. Deterministic build passed with 27 artifacts. Production and full `npm audit` returned zero vulnerabilities. Dockerfile check, Compose config, and local production image build passed; image `watch-tracker:verify` is `sha256:adac979c9e69f05ad39615f1d6e3110c06ada71775c4dae64aa399aeaaf5f818`.
- **CI/remote:** local HEAD, `origin/automation/phase1`, and Core PR #4 are `00cc270222b79f86a7553c7d4b0dedcdf0fdc8a1`. Validate Core run [`32794785528`](https://github.com/bkswindell/watch_tracker/actions/runs/32794785528) passed locked installs, production/full audits, 114 portable tests, isolated PostgreSQL recovery/integrity tests, deterministic build, Docker/Compose checks, application/database image scans, CycloneDX SBOM, and disposable Compose smoke. Template PR #10 remains green at `ec5bcc16517809acce74a1b876dc105c50f3f431`.
- **Deployment/API proof:** the additive `0.09` migration completed successfully without recreating the database container or volume and without changing a credential. Only `watch-tracker-lan-foundation-app-1` was recreated with `--no-deps` on image `sha256:adac979c9e69f05ad39615f1d6e3110c06ada71775c4dae64aa399aeaaf5f818`; app and existing database are healthy. Live `/ready` returned ready. Live `/reset-password` returned the built shell with `Cache-Control: no-store`, `Referrer-Policy: no-referrer`, restrictive CSP, and the expected root element.
- **Browser/audit:** the browser harness still has no supported Chromium-family browser, and no deployment credential was accessed or changed. Latest-head authenticated browser proof was therefore not fabricated. Historical audit remains **FAIL** and no release, tag, or merge is permitted until that proof and final independent audit are complete.
