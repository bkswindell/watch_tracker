# Phase 1 builder status

**Checkpoint:** Authentication/bootstrap payload performance delivery ready for CI; live deployment intentionally held

- **Current deferred-grid checkpoint (2026-08-25):** the full AG Grid runtime now loads only when a user opens a table view (Catalog List, Next Up, or History). The authentication/bootstrap payload falls from **1,367.56 kB / 386.74 kB gzip** to **258.62 kB / 78.70 kB gzip**; the table renderer is an explicit **1,108.96 kB / 308.32 kB gzip** on-demand chunk. Accessible, fixed-height loading states prevent an empty or shifting workspace surface while it arrives. Focus Map remains independently deferred. The portable suite passed all 127 tests; `npm run check` and deterministic build passed (29 artifacts). This is source-only until GitHub CI and app-only deployment are complete; no database or credential operation occurred. Historical Phase 1 audit remains **FAIL**.

- **Prior checkpoint — Next Up artwork resilience deployed; browser harness unavailable:** recommended-item artwork now fails gracefully: an approved `posterUrl` that cannot load is replaced by an accessible, type-initial fallback rather than a broken image. The fallback resets when the recommendation changes and does not fabricate artwork or alter deterministic queue behavior. Commit `9b3b73a92cb605dd2ab6a58b436129fd7b311f6e` is pushed. Focused surface coverage and `npm run check` passed all 126 portable tests; deterministic build passed 28 artifacts; high-severity audit, Compose, and diff validation passed. The app-only `watch-tracker:verify` image (`sha256:20aa8167d22bac7bee96c83010955167839a1a1f572befbdb2f83de5aed534e2`) is deployed; app `/ready` returned 200 and the database container ID/creation time remained unchanged. Browser certification is not claimed because the cron harness cannot start a supported Chromium-family browser. Historical Phase 1 audit remains **FAIL**.

- **Recovery completion hardening:** successful reset now also expires `watch_tracker_session` in the responding browser (`HttpOnly; SameSite=Strict; Path=/; Max-Age=0`). Server-side session revocation remains atomic and authoritative; this removes a stale client cookie when recovery is initiated from an already signed-in browser. The API regression test asserts the precise deletion cookie alongside no-store/no-referrer and three-session revocation.
- **Delivered recovery/auth gate:** the repository-conventional host-admin CLI emits one 256-bit random, 15-minute, fragment-only reset link. PostgreSQL stores only a SHA-256 digest bound to the Tracker instance; issuance atomically supersedes earlier outstanding links. Completion atomically consumes a valid unexpired token, applies the established Argon2id/password policy, revokes every existing session, and consumes a valid token after five policy-invalid submissions.
- **Client-policy parity:** the reset form now rejects a programmatic password longer than 1024 characters before a network request, matching the server’s 15–1024/NUL-free rule instead of consuming a bounded failed-submission attempt. The focused recovery test and the complete 120-test portable suite cover the guard.
- **Invalid-link UX:** a direct `/reset-password` visit or malformed/absent fragment now renders a generic “Password reset unavailable” state with only a return-to-sign-in action—not a password form. It does not submit an empty or malformed token, preserves the fragment-only/no-storage design, and does not disclose whether any reset token exists.
- **Documentation reconciliation:** the authoritative Phase 1 model generator, data dictionary, table budget, logical model, DBML, and DrawDB artifact now name the implemented `password_reset_token` schema (digest-only SHA-256 key, Tracker-instance binding, `TIMESTAMPTZ` expiry/consumption fields, and bounded failures), replacing the obsolete planned `password_recovery_credential` model. A portable regression test parses both machine-readable artifacts and asserts the six ordered fields and Tracker-instance relationship against migrations `0.09`/`0.10`; this prevents future documentation/schema drift without recording a reset secret.
- **Latest verification correction:** recovery tests now execute both CLI outcomes: successful PostgreSQL-backed issuance emits exactly one fragment-only link and persists no plaintext token, while pre-issuance failure emits no stdout and only the generic host-admin error. API coverage proves expired and invalid links have the same public response and headers. Memory and PostgreSQL tests now revoke three concurrent sessions, and PostgreSQL verifies the resulting credential is Argon2id with `m=65536`, `t=3`, `p=1`.
- **Delivery protections:** exact Origin validation rejects userinfo, paths, queries, fragments, and scheme/host/port mismatches. Recovery accepts only the exact two-field JSON contract and a 43-character base64url token. Parser, invalid, expired, superseded, exhausted, reused, malformed, cross-owner, and policy failures remain generic and no-store/no-referrer. The UI removes the URL fragment immediately and never uses browser storage, query strings, analytics, or logs for the token.
- **Current certification refresh:** implementation head `668883d50d4de42f03b8a115f0f41d55990ecb86` passed `npm run check` (formatting, both TypeScript checks, lint, all 120 portable tests, and production builds), `npm run build:deterministic` (27 artifacts), complete and production-only `npm audit --audit-level=high` (zero vulnerabilities), `docker compose config -q`, and `git diff --check`. The Vite build retains its non-failing advisory about a minified client chunk over 500 kB; it remains a performance follow-up, not a security or correctness failure. Local `npm run test:postgres` remains deliberately fail-closed without `TEST_DATABASE_URL`, so no durable database was contacted.
- **CI/remote:** GitHub API confirms Core PR #4 is open and points to implementation commit `668883d50d4de42f03b8a115f0f41d55990ecb86`, matching `origin/automation/phase1`; its new-head validation is pending at this record. Template PR #10 remains open and green at `ec5bcc16517809acce74a1b876dc105c50f3f431`.
- **Live deployment:** rebuilt `watch-tracker:verify` from implementation head `668883d` and recreated only `watch-tracker-lan-foundation-app-1` with `--no-deps`; it is healthy on image `sha256:3b71f60886d97a4a0251f50ed86a1fa0dc7effbeb43416942cae350e33246c08`. In-container `/ready` returned `200`; `/reset-password` returned `200`, `Cache-Control: no-store`, and `Referrer-Policy: no-referrer`. Database container `9167a92718aa2107daa710f998156cc6b4ed47d9e513828bbc70a13d172955d5`, creation time `2026-08-24T16:08:34.899650189Z`, and volume `watch-tracker-lan-foundation_postgres_data` were exactly preserved; no migration, durable-data, or credential operation occurred.
- **Browser evidence:** latest-deployment Microsoft Edge proof used the preserved authenticated session and returned `200`, `authenticated=true`, `setupRequired=false`, `Cache-Control: no-store`, and the rendered workspace. No credential was read or changed. Secret-free screenshot `/tmp/watch-tracker-6e07f50-authenticated.png` has SHA-256 `d0c1602bde010957592ba237f57bb31ca08d235c27cf9dd7c9a794aa50e7e709`.
- **Latest read-only live review:** the deployed application is healthy and `/ready` returned ready. `GET /reset-password` returned `200` with `Cache-Control: no-store`, `Referrer-Policy: no-referrer`, and the restrictive self-only CSP. A synthetic 43-character invalid token submitted with the exact live Origin returned only generic `400 password-reset.invalid` with the same no-store/no-referrer protections. No token issuance, migration, credential, or database operation occurred. The database container remained healthy and was not recreated; only the app container is currently published at `10.18.0.201:3100`. A current cron browser-harness attempt again could not start because no supported Chromium-family browser is running; no new browser certification is claimed.
- **Current Focus Map performance delivery:** React Flow now loads only when the Focus Map surface renders, behind a visible `role="status"` fallback. The deterministic build separates the map into a 190.80 kB (61.86 kB gzip) `FocusGraph` chunk and reduces the entry chunk from 1,551.14 kB (446.08 kB gzip) to 1,361.60 kB (385.29 kB gzip). The new surface-parity regression asserts the lazy import and accessible fallback; the full local gate passes 121 portable tests, deterministic build has 28 artifacts, both dependency audits report zero vulnerabilities, and Compose config validates. The rebuilt app-only container is healthy on image `sha256:8458bf0b8b78d13573c62a3ff51dc3110116d6b7029d02075e7b2d6ee0d37b52`; in-container `/ready` returned `200`. The existing database container ID, creation time, and healthy status are unchanged. This does not touch PostgreSQL, migrations, credentials, or durable state.
- **Workspace navigation delivery:** Focus Map, Catalog, Next Up, History, and Canon Pack now have validated `?view=` deep links. Selecting a navigation surface creates a browser-history entry, browser Back/Forward restores its surface, and transitions close details/context overlays so they cannot leak across workspaces. Unknown view parameters safely fall back to Focus Map; the default map URL remains clean. The focused parity test and full gate now cover this behavior (122 portable tests); deterministic build retained 28 artifacts and both audits report zero vulnerabilities. The verified app-only deployment rebuilt image `sha256:55acecb74bdb59003c12aca7241a2f544e7a16377d96332909c2476149fb9b9e`; its container is healthy and in-container `/ready` returned `200`. The prior live database container ID and creation time are unchanged. The browser harness cannot currently start a Chromium-family browser, so no new browser certification is claimed. No API, migration, PostgreSQL, credential, or durable-state change is included.
- **Canon Pack evidence viewer delivery:** the formerly inert `View verification` control now opens a responsive, read-only modal showing exactly the active workspace API evidence (release, inventory, manifest/checksum identifiers, and validation availability). It marks unavailable evidence honestly and clearly states it does not re-run validation in the browser. The host may download this same non-secret evidence summary as text. The focused surface test and full gate pass (122 portable tests); deterministic build retained 28 artifacts and both audits report zero vulnerabilities. The verified app-only deployment rebuilt image `sha256:5ca5b9cafaec43cd411b1c0a9c5e73e408d19921fb7929eaec0b5a5e4349dcd9`; its container is healthy and in-container `/ready` returned `200`. The existing database container ID and creation time are unchanged. The browser harness cannot currently start a Chromium-family browser, so no new browser certification is claimed. No API, migration, PostgreSQL, credential, or durable-state change is included.
- **Exact-head verification refresh (2026-08-25):** Core implementation head `94712e3389bf143b04b187dd06fb641470eb07dd` is the PR #4 and `origin/automation/phase1` SHA; Validate Core run `32812782394` is green. Locally, `npm run check` passed (122 portable tests), deterministic build passed (28 artifacts), `npm audit --audit-level=high` found zero vulnerabilities, and `git diff --check` plus `docker compose config -q` passed. The local PostgreSQL integration command correctly failed closed because `TEST_DATABASE_URL` is intentionally absent; it made no database connection or state change.
- **Exact deployed artifact review (2026-08-25):** `watch-tracker-lan-foundation-app-1` is healthy and uses image `sha256:5ca5b9cafaec43cd411b1c0a9c5e73e408d19921fb7929eaec0b5a5e4349dcd9`, the existing evidence-viewer deployment. Its in-container `/ready` returned HTTP 200. The cron browser harness still cannot start a supported Chromium-family browser, so no new authenticated/expired/reused/invalid-token browser certification is claimed. The previously recorded browser proof is unchanged; no credential, migration, database container, volume, or OxyGen service was touched.
- **Current Next Up delivery (2026-08-25):** the previously inert `Save view` command now downloads the *visible, filtered* deterministic queue as `watch-tracker-next-up.json`. The artifact includes its generation time, active target, applied filters, truthful totals/remaining minutes, and displayed items; it never claims to persist a server-side queue view. The surface regression protects this behavior. `npm run check` passed with 123 portable tests; deterministic build passed 28 artifacts; high-severity audit, Compose validation, and `git diff --check` passed. The app-only deployment rebuilt `watch-tracker:verify` at image `sha256:326e7835887aae7daec5be321a6d500d1e5efa5f757a09d71b6d22d1bf33e9e5`; replacement app container `f6b618c8dec1333e24e85fd74701d6cb6b7fd64f9b99d235826d8a0365300045` is healthy and its in-container `/ready` returned HTTP 200. The existing database container ID `9167a92718aa2107daa710f998156cc6b4ed47d9e513828bbc70a13d172955d5` and creation time remained unchanged. Browser harness startup remains unavailable, so no new browser certification is claimed.
- **Current Catalog poster-grid delivery (2026-08-25):** Catalog now provides an accessible List/Posters toggle. Posters are rendered from the authenticated workspace snapshot in release order, reuse the existing safe artwork URL path when available, have non-fabricated type-initial fallbacks when artwork is unavailable, honor the existing all-column search, and open the same detail surface as the bounded list. The existing Infinite Row Model remains the list implementation; column controls are unavailable while posters are selected. A focused surface regression protects both modes. At `f7cfe7c`, `npm run check` passed 124 portable tests, deterministic build passed with 28 artifacts, high-severity audit found zero vulnerabilities, and Compose validation plus `git diff --check` passed. The app-only `watch-tracker:verify` image was rebuilt and only `watch-tracker-lan-foundation-app-1` was recreated; it is healthy and its in-container `/ready` returned HTTP 200. Database container `9167a92718aa2107daa710f998156cc6b4ed47d9e513828bbc70a13d172955d5`, creation time, and healthy state were preserved. A current browser-harness attempt could not start a supported Chromium-family browser, so no new browser certification is claimed.
- **Current exact-head verification (2026-08-25):** Core PR #4 remains open at `7cfde6e21bbaadad3668c37d046d188a9d4f81b4`, exactly matching `origin/automation/phase1`; GitHub Validate Core run `32814546571` is green. Template PR #10 remains open and green at `ec5bcc16517809acce74a1b876dc105c50f3f431`. Local `npm run check` passed all formatting/type/lint gates and 124 portable tests, `npm run build:deterministic` passed with 28 artifacts, both production-only and complete high-severity audits found zero vulnerabilities, and Compose plus diff validation passed. PostgreSQL integration/migration identity remains covered by the exact-head green CI run; this host deliberately has no `TEST_DATABASE_URL`, so no live database, credential, volume, or migration was accessed. The currently published app container is healthy; a new browser-harness attempt remains blocked because no supported Chromium-family browser is running, so no unverified browser proof is claimed.
- **Current poster resilience delivery (2026-08-25):** Catalog poster cards now recover gracefully when an otherwise approved artwork URL fails to load: the broken image is replaced in place by the existing non-fabricated, type-initial artwork-unavailable fallback. The List/Posters toggle, authenticated snapshot source, release order, search behavior, bounded list, and shared detail surface are unchanged. A focused surface regression asserts the error path and fallback; `npm run check` passed all formatting/type/lint gates and 124 portable tests, `npm run build:deterministic` passed with 28 artifacts, `npm audit --audit-level=high` found zero vulnerabilities, and Compose plus diff validation passed. The app-only image rebuild is `sha256:3e7ae5191a91456cfd8909cd538f580d30a18489fd0ead67829247d8b636f0af`; only `watch-tracker-lan-foundation-app-1` was recreated and is healthy. Its in-container `/ready` returned HTTP 200. Database container `9167a92718aa2107daa710f998156cc6b4ed47d9e513828bbc70a13d172955d5`, creation time, and healthy state were preserved. Browser harness startup remains unavailable because no supported Chromium-family browser is running, so no new browser certification is claimed. No API, migration, PostgreSQL, credential, volume, or durable-state change is included.
- **Current Canon Pack truthfulness delivery (2026-08-25):** the workspace API now returns the persisted `contractVersion` alongside the release version, and the Canon Pack panel renders that contract value instead of incorrectly repeating the release version. The memory import projection explicitly reports the accepted `0.2.0` contract; PostgreSQL projects `canon_pack_release.contract_version`. Regression coverage proves the protected API response and typed web contract. Commit `325573b59c1464ab001cd45d2708d67f0c6dc602` is pushed and PR #4 is open at that implementation SHA; its Validate Core run `32816456195` is in progress. `npm run check` passed 125 portable tests, formatting, typecheck, lint, and production builds; deterministic build passed 28 artifacts, the high-severity audit reported zero vulnerabilities, and Compose validation passed. App-only deployment rebuilt `watch-tracker:verify` at image `sha256:313844f52588689c4df6e223509fc991fcb4016358ee4ef1fdb29c9868249193` and recreated only `watch-tracker-lan-foundation-app-1`; it is healthy and in-container `/ready` returned HTTP 200. The existing database container creation time (`2026-08-24T16:08:34.899650189Z`) and healthy state were preserved. The failed attempt to create a separate app container without the established Compose project name was immediately removed before start; it had only the read-only initial-password secret bind and no durable volume. No migration, credential, durable data, volume, or database operation occurred.
- **Current catalog filter delivery (2026-08-25):** Catalog now has compact Type and Viewing state command filters which apply consistently to the bounded list and Posters presentation. The poster result summary identifies active filters and its empty state resets the complete filter set. Commit `7ad6143c0a59bfab10fce04b995124824a6819ba` is pushed; PR #4 points to it and Validate Core run `32816988540` is queued. `npm run check` passed all 125 portable tests, deterministic build passed 28 artifacts, both high-severity audits reported zero vulnerabilities, and Compose/diff validation passed. The app-only `watch-tracker:verify` image is `sha256:c91672946b73cf02abdf54418763685b56b1c0bc4e84f0e297c315b00b1c4efa`; replacement app container `3c55c9d5b6907332c3f6d2db372bf495dd7b4f086fd0ed73f6c02126b1c535e0` is healthy and in-container `/ready` returned HTTP 200. The existing database container ID and creation time are unchanged. Browser harness startup remains unavailable, so no browser certification is claimed.
- **Release state:** the recovery gate passes, but the historical overall Phase 1 audit remains **FAIL** pending broader Phase 1 closeout and exact-final-diff governance. PRs remain open; no release, tag, default-branch merge, credential change, force-push, destructive durable-data operation, or OxyGen/port 3000 change occurred.
