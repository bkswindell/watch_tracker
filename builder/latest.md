# Phase 1 builder status

**Checkpoint:** `8103dd0772ab9e0d5623e789b4559bd98e09848a` — `fix(security): prevent caching auth bootstrap`

- **Delivered auth/session gate:** exact scheme/host/port Origin validation in addition to token CSRF; bounded five-attempt/15-minute per-client login throttling; authenticated CSRF-protected logout with server-side session invalidation and cookie expiry; 30-day sliding idle and 90-day absolute session lifetimes; Argon2id fresh credentials at `m=65536`, `t=3`, `p=1` with the accepted password policy. Login throttle state is capped at 10,000 active entries.
- **New hardening:** `/api/bootstrap`, `/api/login`, and `/api/logout` return `Cache-Control: no-store`. This prevents caches retaining the bootstrap/session CSRF material or authentication responses.
- **Validation:** focused auth/SQL invocation passed all 10 targeted tests; `npm run check` passed all 106 portable tests; deterministic build passed (25 artifacts); `npm audit --omit=dev --audit-level=high` reported 0 vulnerabilities; production image build passed.
- **Deployment/API proof:** rebuilt image `watch-tracker@sha256:c0f50ab8dcd861f7e516a02ca8e52eda5935d45953bec61383fbf1ca91b56c22`; recreated only `watch-tracker-lan-foundation-app-1` with `--no-deps`. The existing database container remained healthy and untouched. The deployed app is healthy; `/ready` returned ready and credential-free `GET /api/bootstrap` returned `Cache-Control: no-store`.
- **CI/remote:** Validate Core for this implementation checkpoint has not yet been requested by the push. Prior Core head `89555034495238e5b9b0206bc56228a648238b58` passed run `32784974478`; the branch prior to this implementation was `e44170e67879de5861d07ebc8cdc6f66ae2159e6`. Template PR #10 remains green at `ec5bcc16517809acce74a1b876dc105c50f3f431`.
- **Browser proof:** no deployment credential was accessed. Authenticated browser proof remains unavailable in this cron environment and was not fabricated; API proof above covers this API-only header slice.
- **Remaining accepted auth gate:** host-controlled recovery. The exact decision needed is an approved host-managed delivery channel (for example SMTP relay or help-desk/admin-mediated reset) and the user-facing recovery UX. No host credentials, PostgreSQL container, or volume were changed to infer it.
- **Historical audit:** remains **FAIL** until host-controlled recovery and every accepted auth/session gate passes.
