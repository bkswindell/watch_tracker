# Phase 1 builder status

**Checkpoint:** `c8b345483adad3d5a2df3bce7b9fc3a27003d6bc` — `fix(auth): retain reset token after retryable failure`

- **Delivered recovery/auth gate:** the repository-conventional host-admin CLI emits one 256-bit random, 15-minute, fragment-only reset link. PostgreSQL stores only a SHA-256 digest bound to the Tracker instance; issuance atomically supersedes earlier outstanding links. Completion atomically consumes a valid unexpired token, applies the established Argon2id/password policy, revokes every existing session, and consumes a valid token after five policy-invalid submissions.
- **Latest correction:** the reset UI now keeps the fragment-derived token only in component memory after a generic API failure, allowing a policy-corrected or transient retry without reopening the link. It clears the token immediately after the server confirms atomic consumption. A malformed IPv4 loopback base URL also has explicit CLI regression coverage.
- **Delivery protections:** exact Origin validation rejects userinfo, paths, queries, fragments, and scheme/host/port mismatches. Recovery accepts only the exact two-field JSON contract and a 43-character base64url token. Parser, invalid, expired, superseded, exhausted, reused, malformed, cross-owner, and policy failures remain generic and no-store/no-referrer. The UI removes the URL fragment immediately and never uses browser storage, query strings, analytics, or logs for the token.
- **Validation:** focused recovery tests passed 5/5; `npm run check` passed all 116 portable tests plus formatting, typecheck, lint, and production builds. PostgreSQL integration passed 2/2 against a disposable PostgreSQL 17 instance. Production/full high-severity audits reported zero vulnerabilities; deterministic production build passed with 27 artifacts; Dockerfile and Compose checks passed. Trivy 0.74.0 reported zero HIGH/CRITICAL fixed findings in the application and database images.
- **CI/remote:** Core PR #4 remains open and clean at exact implementation SHA `c8b345483adad3d5a2df3bce7b9fc3a27003d6bc`, matching `origin/automation/phase1`; Validate Core run [`32802340817`](https://github.com/bkswindell/watch_tracker/actions/runs/32802340817) passed every gate. Template PR #10 remains open and green at `ec5bcc16517809acce74a1b876dc105c50f3f431`.
- **Live deployment:** only the app was rebuilt and recreated with `--no-deps` on image `sha256:3b6848d51e846a1872f6e9d02e98db480ed23faf228dc3a4049d437df9167329`. It is healthy and `/ready` is ready. Database container `9167a92718aa2107daa710f998156cc6b4ed47d9e513828bbc70a13d172955d5`, creation time `2026-08-24T16:08:34.899650189Z`, volume `watch-tracker-lan-foundation_postgres_data`, migration state, durable data, and credentials were preserved.
- **Browser evidence:** latest-implementation Microsoft Edge 151 authenticated with the existing private credential and displayed the Administrator workspace. Secret-free retry proof used an invalid synthetic token: the fragment was removed, referrer and browser storage stayed empty, two generic `400` submissions were possible from the same page, and password fields were cleared after each failure. Screenshot SHA-256 values: authenticated `d0c1602bde010957592ba237f57bb31ca08d235c27cf9dd7c9a794aa50e7e709`; retry `e866b03eb77db1f98b796391fe2f0a281056f10c8333aa875ba056b5f9f916be`.
- **Release state:** the recovery gate passes, but the historical overall Phase 1 audit remains **FAIL** pending broader Phase 1 closeout and exact-final-diff governance. PRs remain open; no release, tag, default-branch merge, credential change, force-push, destructive durable-data operation, or OxyGen/port 3000 change occurred.
